import React from 'react';

const Header = () => {
  return (
    <div
      className="ui header  secondary pointing menu"
      style={{
        display: 'flex',
        justifyContent: 'center',
        marginTop: '20px',
        marginBottom: '20px',
      }}
    >
      Harry Potter Database
    </div>
  );
};

export default Header;
